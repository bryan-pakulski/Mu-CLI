"""Browser GUI for mucli — `mucli --gui` boots a daemonized FastAPI
server on port 30311 (default) and frees the terminal.

Public entry: :func:`mu.gui.launcher.run_gui`. Everything else is
implementation detail.
"""
